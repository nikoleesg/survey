<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    /**
     * Run the migrations.
     */
    public function up(): void
    {
        Schema::create(config('survey.table_prefix').'answers', function (Blueprint $table) {
            $table->id();
            $table->foreignId('sample_id')->constrained(config('survey.table_prefix').'samples')->cascadeOnDelete();
            $table->foreignId('variable_id')->constrained(config('survey.table_prefix').'variables')->cascadeOnDelete();
            $table->json('result');
            $table->timestamps();

            $table->unique(['sample_id', 'variable_id']);
        });
    }

    /**
     * Reverse the migrations.
     */
    public function down(): void
    {
        Schema::dropIfExists(config('survey.table_prefix').'answers');
    }
};
